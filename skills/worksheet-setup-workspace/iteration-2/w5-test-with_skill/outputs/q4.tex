\documentclass[main.tex]{subfiles}
\begin{document}

\qs{Question 4}{
(10 pts) Use your result from the preceding exercise to show that, if the weight updates over an episode were computed on each step but not actually used to change the weights ($\mathbf{w}$ remained fixed), then the sum of $\text{TD}(\lambda)$'s weight updates would be the same as the sum of the off-line $\lambda$-return algorithm's updates. You will probably need the summation rule given on page 310.
}

\sol

\end{document}
