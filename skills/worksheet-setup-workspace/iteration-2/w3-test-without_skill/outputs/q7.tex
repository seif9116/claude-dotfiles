\documentclass[main.tex]{subfiles}
\begin{document}

\qs{Question 7}{
(5 pts) The pseudocode in the box on page 251 updates $\bar{R}_t$ using $\delta_t$ as an error rather than simply $R_{t+1} - \bar{R}_t$. Both errors work, but using $\delta_t$ is better. To see why, consider the ring MRP of three states from Question 6. The estimate of the average reward should tend towards its true value of 2. Suppose it was already there, was held stuck there, and the state-value estimates were also correct. What would the sequence of $R_{t+1} - \bar{R}_t$ errors be? What would the sequence of $\delta_t$ errors be? Which error sequence would produce a more stable estimate of the average reward if the estimate were allowed to change in response to the errors? Why?
}

\sol

\end{document}
